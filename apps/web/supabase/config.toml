# A string used to distinguish different Supabase projects on the same host. Defaults to the working
# directory name when running `supabase init`.
project_id = "kinder-crm"

[api]
# Port to use for the API URL.
# Avoid 54321–54326 on Windows: Hyper-V often reserves 54268–54467, blocking Docker bind.
port = 55521
# Schemas to expose in your API. Tables, views and stored procedures in this schema will get API
# endpoints. public and storage are always included.
schemas = ["public", "storage", "graphql_public"]
# Extra schemas to add to the search_path of every request. public is always included.
extra_search_path = ["public", "extensions"]
# The maximum number of rows returns from a view, table, or stored procedure. Limits payload size
# for accidental or malicious requests.
max_rows = 1000

[db.seed]
enabled = true
sql_paths = ["./seed.sql"]

[db]
# Port to use for the local database URL.
# Avoid 54322–54467 on Windows: Hyper-V often reserves overlapping ranges, blocking Docker bind.
port = 55532
# The database major version to use. This has to be the same as your remote database's. Run `SHOW
# server_version;` on the remote database to check.
major_version = 17

[studio]
# Port to use for Supabase Studio.
port = 55523

# Email testing server. Emails sent with the local dev setup are not actually sent - rather, they
# are monitored, and you can view the emails that would have been sent from the web interface.
[inbucket]
# Port to use for the email testing server web interface.
port = 55524
smtp_port = 55525
pop3_port = 55526

[storage]
# The maximum file size allowed (e.g. "5MB", "500KB").
file_size_limit = "50MiB"

[auth]
# The base URL of your website. Used as an allow-list for redirects and for constructing URLs used
# in emails.
site_url = "http://localhost:3000"
# A list of *exact* URLs that auth providers are permitted to redirect to post authentication.
additional_redirect_urls = [
  "http://localhost:3000",
  "http://localhost:3000/auth/callback",
  "http://localhost:3000/update-password",
  "http://127.0.0.1:3000",
  "http://127.0.0.1:3000/auth/callback",
  "http://127.0.0.1:3000/update-password",
]
# How long tokens are valid for, in seconds. Defaults to 3600 (1 hour), maximum 604,800 seconds (one
# week).
jwt_expiry = 3600
# Allow/disallow new user signups to your project.
enable_signup = true

[auth.email]
# Allow/disallow new user signups via email to your project.
enable_signup = true
# Invite / magic-link / recovery OTP validity (seconds). Max 86400 (24h) per Supabase.
otp_expiry = 86400
# If enabled, a user will be required to confirm any email change on both the old, and new email
# addresses. If disabled, only the new email is required to confirm.
double_confirm_changes = true
# If enabled, users need to confirm their email address before signing in.
enable_confirmations = true

# Use an external OAuth provider. The full list of providers are: `apple`, `azure`, `bitbucket`,
# `discord`, `facebook`, `github`, `gitlab`, `google`, `keycloak`, `linkedin`, `notion`, `twitch`,
# `twitter`, `slack`, `spotify`, `workos`, `zoom`.
[auth.external.apple]
enabled = false
client_id = ""
secret = ""
# Overrides the default auth redirectUrl.
redirect_uri = ""
# Overrides the default auth provider URL. Used to support self-hosted gitlab, single-tenant Azure,
# or any other third-party OIDC providers.
url = ""

# redirect_uri must match an "Authorized redirect URI" in Google Cloud (exact string).
# Same host/port as [api] + /auth/v1/callback — keep in sync with NEXT_PUBLIC_SUPABASE_URL.
[auth.external.google]
enabled = true
client_id = "env(GOOGLE_CLIENT_ID)"
secret = "env(GOOGLE_CLIENT_SECRET)"
redirect_uri = "http://127.0.0.1:55521/auth/v1/callback"
url = ""

[auth.email.template.invite]
subject = "Lời mời tham gia Kinder CRM"
content_path = "./supabase/templates/invite-user.html"

[auth.email.template.confirmation]
subject = "Xác nhận email — Kinder CRM"
content_path = "./supabase/templates/confirm-email.html"

[auth.email.template.recovery]
subject = "Đặt lại mật khẩu — Kinder CRM"
content_path = "./supabase/templates/reset-password.html"

[auth.email.template.email_change]
subject = "Xác nhận đổi email — Kinder CRM"
content_path = "./supabase/templates/change-email-address.html"

[auth.email.template.magic_link]
subject = "Đăng nhập — Kinder CRM"
content_path = "./supabase/templates/magic-link.html"

[analytics]
enabled = false